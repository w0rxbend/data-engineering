# Kafka Streams logs a lot at INFO level while it starts up, restores state
# stores and shuts down again. The tests start a fresh TopologyTestDriver per
# test case, so INFO would bury the actual assertion failures.
org.slf4j.simpleLogger.defaultLogLevel=warn
